# 작업일지 — 2026년 4월 6일

---

## 1. analyze_hybrid.py 실제 분석 실행 및 데모 데이터 갱신

### 내용
- `S1 철수_fixed.wav` + `S2 영희_fixed.wav`를 이용해 `analyze_hybrid.py` 실제 실행
- 올바른 ref 매핑 확인: iOS ref = `dating_SPEAKER_01.wav`(영희), Android ref = `dating_SPEAKER_00.wav`(철수)
- 분석 결과: dropout_count=9, severity="심각", iOS MOS=3.969, Android MOS=3.141
- 리포트 저장: `reports/demo_benchmark_20260406.html`

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src/hooks/useTcRunner.ts` | `DEMO_RESULT.dropoutSeverity` "없음" → "심각", `dropoutReportPath` null → 실제 경로 |

---

## 2. 앱 데모 세션 표시 로직 완전 격리

### 문제
- `injectDemoIfAbsent()`가 DEMO 데이터를 React state에 삽입 → `useEffect`가 localStorage에 저장
- 이후 실제 테스트(439~441번) 결과에 DEMO 데이터가 섞여 HTML 보고서까지 오염

### 원인 분석
1. `useState` 초기화 시 DEMO를 state에 포함 → localStorage 오염
2. `clearResults()` 가 `setTcResults([DEMO_RESULT])` 로 DEMO를 state에 저장
3. `html_report.py`의 `build_html()` 안에 `build_benchmark_pin_html()` 호출이 삽입돼 있어 모든 보고서에 Ground Truth 섹션이 표시됨

### 수정 내용
- DEMO는 `return` 시점 **화면 표시 전용**으로만 주입 — state·localStorage와 완전 격리
- `useState` 초기화: localStorage 로드 시 DEMO runId 필터링 + 즉시 재저장으로 기존 오염 정리
- `clearResults()`: `setTcResults([])` 로 원복
- `html_report.py`: `build_html()` 내 `{build_benchmark_pin_html()}` 호출 제거

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src/hooks/useTcRunner.ts` | `useState` 초기화·`clearResults`·`deleteSelected`·`return` 시 display 로직 수정 |
| `html_report.py` | `build_html()` 내 `build_benchmark_pin_html()` 호출 제거 |

---

## 3. 데모 세션 화면 표시 위치 수정

### 문제
- `DashboardView`가 세션 목록을 `[...activeSessions].reverse()`로 렌더링
- DEMO를 배열 앞에(`[DEMO, ...]`) 넣으면 reverse 후 화면 최하단으로 이동 → 화면에서 안 보임

### 수정 내용
- DEMO를 배열 **끝**에(`[..., DEMO]`) 붙이도록 변경 → reverse 후 화면 최상단 표시

---

## 4. 데모 세션 특수 스타일 제거

### 문제
- 녹색 테두리, 벤치마크 배너, 상세 결과 섹션이 데모 세션에만 표시 → 실제 테스트와 이질감

### 수정 내용
- `isDemo` 관련 특수 스타일 전부 제거 (녹색 border, 배너 div, 음단절 상세 섹션)
- 일반 세션과 동일하게 표시

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src/components/DashboardView.tsx` | isDemo 특수 스타일 블록 제거 |

---

## 5. 삭제 확인 팝업 UI 개선

### 수정 내용
- 팝업 크기 확대: `min-width 280 → 380px`, `padding 24 → 36px`
- 제목 폰트: `15px → 20px`, 색상 순백(`#ffffff`)
- 본문 폰트: `13px → 16px`, 밝은 회색(`#c8d0da`)
- 버튼 전용 클래스 `btn-confirm` 추가: `padding 10px 28px`, `font-size 15px`, `font-weight 600`
- 전체 삭제 / 선택 삭제 팝업 두 곳 모두 적용

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src/App.css` | `.confirm-dialog`, `.confirm-title`, `.confirm-body`, `.btn-confirm` 스타일 확대 |
| `sound-test-app/src/components/DashboardView.tsx` | 팝업 버튼 `btn-xs` → `btn-confirm` 교체 |

---

## 6. TC_01/TC_02 ref 매핑 버그 수정

### 문제
- Rust `test_cmd.rs`의 화자별 정답지 매핑이 실제 하드웨어 연결과 반대로 구현됨
- 실제 설정: S1 출력 = CONNECT 6 [1] → iPhone 입력, S2 출력 = CONNECT 6 [2] → Android 입력
  - **TC_01**: iPhone(S1 발신) → Android가 S1 수신 녹음, Android(S2 발신) → iOS가 S2 수신 녹음
  - **TC_02**: Android(S1 발신) → iOS가 S1 수신 녹음, iPhone(S2 발신) → Android가 S2 수신 녹음
- 기존 코드: TC_01 android=S2, ios=S1 **(반대)**

### 수정 내용
```
TC_01: ref_for_android = S1(철수), ref_for_ios = S2(영희)
TC_02: ref_for_android = S2(영희), ref_for_ios = S1(철수)
```

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src-tauri/src/test_cmd.rs` | TC_01/TC_02 ref 매핑 로직 및 주석 수정 |

---

## 7. 오염된 HTML 리포트 재분석·복구

### 대상
| 리포트 파일 | 회차 | TC | 원인 |
|-------------|------|----|------|
| `hybrid_report_1775465535.html` | 439 | TC_02 | 잘못된 ref 매핑 + Ground Truth 섹션 삽입 |
| `hybrid_report_1775465884.html` | 440 | TC_01 | 동일 |
| `hybrid_report_1775466240.html` | 441 | TC_01 | 동일 |
| `hybrid_report_1775468751.html` | 443 | TC_01 | iOS/Android 음원 내용이 실제 반대(케이블 미스) |

### 재분석 결과
| 회차 | iOS MOS | Android MOS | dropout | 비고 |
|------|---------|-------------|---------|------|
| 439 (TC_02) | 1.271 | 1.406 | 10 | 심각 (실제 음원 불량) |
| 440 (TC_01) | 1.265 | 1.280 | 10 | 심각 (실제 음원 불량) |
| 441 (TC_01) | 1.315 | 1.300 | 10 | 심각 (실제 음원 불량) |
| 443 (TC_01) | 3.299 | 4.169 | 7  | 정상 (케이블 역전 보정 후 재분석) |

---

## 8. 잔여 이슈

- 439~441번은 재분석 후에도 MOS 1.2~1.4, dropout 10건으로 실제 음원 품질이 불량한 상태
  - 데모 데이터 오염이 아닌 해당 시간대 실제 녹음 품질 문제로 판단
- 443번은 케이블 연결이 역전된 상태에서 테스트된 것으로 확인, 올바른 ref로 재분석 완료

---

---

## 9. TC_01/TC_02 ref 매핑 재확인 및 최종 확정

### 배경
- TC_01 재테스트(음원 362, 363) 결과 MOS 1.276/1.259로 불량 → ref가 여전히 뒤바뀐 것으로 의심
- 파형 이미지에서 오프셋이 -4.859s / +5.840s로 비정상적으로 큼 → ref-녹음 화자 불일치 징후

### 원인 분석 (Python 직접 검증)
```python
iOS vs S00(철수):  offset = -5.706s  ← 멀리 떨어짐 (불일치)
iOS vs S01(영희):  offset = +4.899s  ← 멀리 떨어짐 (불일치)
# 에너지 확인
0-6.3s (철수 발화)   iOS mean dB: -38.5  ← 소리 있음
7-12.4s (영희 발화)  iOS mean dB: -120.0 ← 완전 묵음
```
→ **iOS 녹음에는 철수 음성이 녹음됨** → iOS ref = S00(철수) 가 맞음

이전 세션에서 "TC_01: Android=S1(철수), iOS=S2(영희)" 로 수정했던 것이 **반대**였음을 최종 확인.

### 최종 확정 매핑
| TC | Android 녹음 | iOS 녹음 | ref_for_android | ref_for_ios |
|----|-------------|---------|-----------------|-------------|
| TC_01 | S2(영희) | S1(철수) | SPEAKER_01 | SPEAKER_00 |
| TC_02 | S1(철수) | S2(영희) | SPEAKER_00 | SPEAKER_01 |

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `sound-test-app/src-tauri/src/test_cmd.rs` | TC_01 → android=S2, ios=S1 / TC_02 → android=S1, ios=S2 로 재수정 |

---

## 10. frame-level 음단절 scan 오탐 수정

### 문제
- test 363: `max_corr = +0.975` (정상)인데도 `dropped = True` 판정
- HTML 보고서에서 상관계수 -0.975로 표시되어 음단절 오탐처럼 보였으나, 내부 값은 **+0.975가 맞음**
- `analyze_by_script()` 내 2단계 frame-level partial drop scan이 실제로 오탐 유발

### 원인
- VoIP 코덱(AMR/EVS) 경유 후 DTX(Discontinuous Transmission) 정적 프레임 또는 자연 발화 포즈(쉬어가기)를 `peak < 0.001` 조건으로 묵음 스팬으로 판정
- 합산 묵음 ≥ 150ms → `partial_drop = True` → `dropped = True` 로 덮어씀
- `max_corr = 0.975` 이미 오디오 존재를 증명하는데 frame scan이 override

### 수정
```python
# 기존: if not dropped:
# 수정: max_corr >= CORR_GOOD(0.75)이면 frame scan 완전 스킵
if not dropped and max_corr < CORR_GOOD:
    # frame-level scan ...
```

### 수정 결과
| 측정 항목 | 수정 전 | 수정 후 |
|-----------|---------|---------|
| iOS 음단절 | 5/5건 (100%) | **0/5건 (0%)** |
| Android 음단절 | 2/5건 (40%) | **0/5건 (0%)** |
| iOS MOS | 1.276 | 3.550 |
| Android MOS | 1.259 | 4.142 |

### 변경 파일
| 파일 | 변경 내용 |
|------|-----------|
| `script_gap_detector.py` | line 449: `if not dropped:` → `if not dropped and max_corr < CORR_GOOD:` |

---

## 오늘의 작업 완료

- [x] TC_01 재시작 후 ref 매핑 수정 검증 → 최종 확정
- [x] frame-level 음단절 오탐 수정 → 정상 음원 0건 판정
- [x] test 362, 363 보고서 재생성 완료
